<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kanglian.healthcare.back.dal.dao.PaymentOrderDao">
	<select id="getByOrderNo" resultMap="BaseResultMap" parameterType="java.lang.String">
		select
		<include refid="columns"/>
		from payment_order t
		where t.order_no = #{orderNo,jdbcType=VARCHAR}
	</select>
	
	<select id="getByGoodsId" resultMap="BaseResultMap" parameterType="java.lang.String">
		SELECT
		<include refid="columns"/>
		FROM
			payment_order t
		LEFT JOIN payment_order_item t1 ON t.order_id = t1.order_id
		WHERE t1.goods_id = #{goodsId,jdbcType=VARCHAR}
	</select>
	
</mapper>
