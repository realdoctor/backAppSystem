<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kanglian.healthcare.back.dal.dao.UploadContentDao">
	<select id="getByPubId" resultMap="BaseResultMap" parameterType="java.lang.String">
		select
		<include refid="columns"/>
		from upload_content t
		where pub_id=#{pubId,jdbcType=VARCHAR}
	</select>

	<delete id="deleteByPubId" parameterType="java.lang.String">
		delete from upload_content where order_id=#{orderId,jdbcType=VARCHAR}
	</delete>

	<update id="updateByPubId" parameterType="com.kanglian.healthcare.back.dal.pojo.UploadContent">
		update upload_content
		<set>
		  <if test="userId != null">user_id=#{userId,jdbcType=INTEGER},</if>
		  <if test="type != null">type=#{type,jdbcType=INTEGER},</if>
		  <if test="content != null">content=#{content,jdbcType=VARCHAR},</if>
		  <if test="src != null">src=#{src,jdbcType=VARCHAR},</if>
		  <if test="remark != null">remark=#{remark,jdbcType=VARCHAR},</if>
		  <if test="addTime != null">add_time=NOW(),</if>
		</set>
		where pub_id=#{pubId,jdbcType=VARCHAR}
	</update>
</mapper>
