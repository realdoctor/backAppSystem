<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kanglian.healthcare.back.dal.dao.UploadContentDao">
	<select id="getByUUId" resultMap="BaseResultMap" parameterType="java.lang.String">
		select
		<include refid="columns"/>
		from upload_content t
		where t.id = #{id,jdbcType=INTEGER}
	</select>

	<delete id="deleteByUUId" parameterType="java.lang.String">
		delete from upload_content where order_id=#{orderId,jdbcType=VARCHAR}
	</delete>

	<update id="updateByUUId" parameterType="com.kanglian.healthcare.back.dal.pojo.UploadContent">
		update upload_content
		<set>
		  <if test="userId != null">user_id=#{userId,jdbcType=INTEGER},</if>
		  <if test="type != null">type=#{type,jdbcType=INTEGER},</if>
		  <if test="content != null">content=#{content,jdbcType=VARCHAR},</if>
		  <if test="path != null">path=#{path,jdbcType=VARCHAR},</if>
		  <if test="remark != null">remark=#{remark,jdbcType=VARCHAR},</if>
		  <if test="addTime != null">add_time=NOW(),</if>
		</set>
		where order_id=#{orderId,jdbcType=VARCHAR}
	</update>
</mapper>
